# Репы-потребители единого CI-гейта. Формат: owner/repo <python|node|terraform>
# Вольты CI не подключают с 2026-09-26: вольт — папка с документами, секреты ловит хук до коммита.
# terraform = инфраструктурный код (OpenTofu), гейт ci-terraform.yml.
# Новая репа флота → строка сюда + connect-project (штампует стабы).
#
# Стабы зовут рецепты подвижным тегом @v1 (канон CI 2026-09-19): правка рецепта доезжает
# движением тега, массовый бамп пинов не нужен. Скрипт `bump-stubs.sh` был как раз для него и
# удалён 2026-09-23 — он искал в стабах SHA (`ci-<stack>.yml@[a-f0-9]+`), а там давно `@v1`,
# так что не срабатывал НИ В ОДНОЙ репе и открыл бы 14 пустых PR. Вместе с ним ушла колонка
# `stub=`, у которой он был единственным читателем; заодно из неё исчезла строка, объявлявшая
# dippstack/dippstack «своим гейтом» — у него три джоба, и третий зовёт ci-node.yml@v1.
#
# bali-smm убраны 2026-09-23: три репозитория архивированы 09-22, раннеры орги сняты.
dippstack/ais                           python
marketspace-ai/marketspace-platform     python
yan-app/yan-monorepo                    node
dippstack/dippstack                     node
dippstack/dippcloud                     terraform
